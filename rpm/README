
This directory contains a Makefile for creating an lsvmtools RPM package.

There are two steps:

    (1) Copy a signed version of LSVMLOAD to:

        lsvmtools/lsvmload/lsvmload.signed.efi

    (2) Type "make" in this directory.

The resulting RPM package will appear in this directory.

